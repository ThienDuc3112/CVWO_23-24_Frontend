import { Container, Typography } from "@mui/material";

const Rules = () => {
  return (
    <Container>
      <Typography variant="h1">Rules</Typography>
      <Typography>Don't do bad stuff</Typography>
    </Container>
  );
};

export default Rules;
